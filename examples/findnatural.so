load "ling/phonology.so";
load "vector.so";
load "html.so";

var phones = {'m', 'p', 'b', 'g', 'ŋ', 'ɲ', 'ɴ', 'ɱ'};

var naturalsets = Phonology.findNaturalSets(_base, phones);

println("Input Phones:");
println(phones);
println("");
println("Natural Sets:");
print(Format.table(
	null,
	naturalsets.map((ref set) => {
    	return {set, Phonology.naturalize(_base, set)};
	})
));